// IIFE - Immediately invoked function expression

((name) => {
    console.log(`Bem vindo(a) ${name}`)
})("Bruno") //não existe uma forma de chamar essa função novamente no código, pois ela é usada imediatamente!